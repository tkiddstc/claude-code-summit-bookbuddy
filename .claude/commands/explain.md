Explain how the request lifecycle works for $ARGUMENTS.

Trace the full path from the HTTP request to the response:
1. Which route file handles it?
2. What middleware runs?
3. What database queries execute?
4. Which view template renders?
5. What data gets passed to the template?

Keep it concise — one paragraph per step. Use actual file paths and line numbers.
